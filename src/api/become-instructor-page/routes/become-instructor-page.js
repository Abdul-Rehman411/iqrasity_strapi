'use strict';

/**
 * become-instructor-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::become-instructor-page.become-instructor-page');
