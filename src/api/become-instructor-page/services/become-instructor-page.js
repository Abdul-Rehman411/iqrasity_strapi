'use strict';

/**
 * become-instructor-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::become-instructor-page.become-instructor-page');
